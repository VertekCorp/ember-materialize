import Ember from 'ember';
import EmInput from './em-input';
import layout from '../templates/components/em-phone-input';
const { Component, isPresent, computed, defineProperty } = Ember;

export default EmInput.extend({
  layout
});
